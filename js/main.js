/* ============================================================
   js/main.js
   Hàm đặc thù của app VELA_VTTB — hiện chưa có gì vì logic nghiệp vụ
   (nhận xe, hóa đơn, thanh toán, thiết bị...) sẽ build ở Bước 6+ trong
   từng module riêng. Giữ file này làm nơi tập trung helper dùng chung
   giữa NHIỀU module nghiệp vụ (không đủ tổng quát để đưa vào utils.js).
   ============================================================ */
